# Midnight network to target: undeployed | preview | preprod | mainnet
VITE_MIDNIGHT_NETWORK_ID=preprod

# Overrides (optional). When unset, preprod/mainnet defaults are used and a
# connected wallet's reported endpoints take precedence at runtime.
# VITE_INDEXER_URI=https://indexer.preprod.midnight.network/
# VITE_INDEXER_WS_URI=wss://indexer.preprod.midnight.network/
VITE_PROOF_SERVER_URI=http://127.0.0.1:6300
# VITE_ZK_ARTIFACTS_BASE_URL=http://localhost:8080/managed/shadowbid
